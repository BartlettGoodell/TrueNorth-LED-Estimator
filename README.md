# LED Video Wall Estimator

Drag/resize your LED wall in 0.5m cabinet increments to instantly calculate total cost, cost per m², and order-level pricing.

## Run locally
pip install streamlit pandas numpy plotly
streamlit run led_wall_estimator.py
